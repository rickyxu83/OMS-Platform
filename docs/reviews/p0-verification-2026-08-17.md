# P0 修复验证清单（2026-08-17，测试服）

> 对应提交：c086859（P0 修复）+ 4b8cacf（审查报告）。部署：测试服 admin（备份 admin-site-20260817073440）。
> 所有页面验证前先强制刷新（Cmd+Shift+R）避免旧 chunk 缓存。

## 1. 品牌色统一（#582B8B 一色到底）

- **登录页**：标题 "OMS Platform"、登录按钮、输入框聚焦边框/光环、语言切换选中项均为深紫；与登录后侧边栏选中项、主按钮**同一个紫色**，不再是两种紫。
- **登录后任意页面**：主按钮（新增/提交）、侧边栏选中项、焦点环均为深紫 #582B8B。
- **Dashboard 地图**：点击客户点位，弹窗里"年服务 N 次"为紫色（跟随主色）。

## 2. Toast 提示

- 桌面端：触发任意操作（如刷新列表），toast 仍为右下角，但变为**亮色底**（原来是暗色）。
- 手机/窄屏（<1024px）：触发 toast（如切换语言），toast 从**顶部居中**弹出，不再遮挡底部导航。

## 3. 统计数字千分位

- Dashboard / 工单处理 / 客户档案 / 设备资产 / 巡检计划 / 月报导出 / 操作审计 / 考勤管理：统计卡数字超过 999 时显示千分位（如 1,234）。

## 4. AI 运营总结图标

- Dashboard 右上角"AI 运营总结"按钮图标为 ✨（Sparkles），不再是下载图标；弹窗内"生成总结"按钮同样为 ✨。工单列表的"导出"按钮保持下载图标不变。

## 5. 空态组件

- 把列表筛选到无结果（如工单处理搜一个不存在的关键词）：出现**图标 + 文案 + 说明/按钮**的统一空态，不再是一行灰字。
- 工单处理/客户档案/巡检计划/维保方目录的空态带"新增"按钮，点击直接打开新建弹窗。
- 待办中心"待我处理"为空时显示"待办都处理完了"。

## 6. 动画降级（prefers-reduced-motion）

- macOS：系统设置 → 辅助功能 → 显示器 → 勾选"减少动态效果"后刷新页面：列表行入场、统计数字浮现、路由转场、测试服横幅扫光全部静止；三点加载指示器保持静态可见。

## 7. 登录页无障碍

- 输错密码：错误红框出现（屏幕阅读器可自动播报，role=alert）。
- 密码框眼睛按钮：鼠标悬停/聚焦时有中文"显示密码/隐藏密码"语义。
- 浏览器密码管理器可正常识别账号/密码字段（补了 name 属性）。

## 8. 待办中心键盘操作

- 待办中心列表行：Tab 键可聚焦行，Enter/空格直接打开详情（原来只能鼠标点）。

## 9. 版本号

- 登录页底部与侧边栏左下角"系统版本"显示 **26.0817.2229**。
