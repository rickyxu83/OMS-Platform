{
  "name": "oms-platform",
  "private": true,
  "scripts": {
    "typecheck": "cd frontend-admin && tsc --noEmit",
    "lint:boundaries": "depcruise backend/src frontend-admin/src",
    "check": "npm run typecheck && npm run lint:boundaries && npm --prefix backend run check",
    "test": "npm run check && npm --prefix frontend-admin run test:mr && npm --prefix backend test"
  },
  "devDependencies": {
    "dependency-cruiser": "^18.1.1",
    "typescript": "^5.9.3"
  }
}
